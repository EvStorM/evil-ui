<checkbox
        class="{{className}}"
        value="{{value}}"
        checked="{{checked}}"
        disabled="{{disabled}}"
        defaultChecked="{{defaultChecked}}"
        id="{{id}}"
        onChange="onChange"
        label="{{label}}"
>
    <slot></slot>
</checkbox>
