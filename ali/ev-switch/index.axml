<switch class="am-switch {{checkedCls}}" size="{{size}}"
        name="{{name}}" checked="{{checked}}" disabled="{{disabled}}"
        color="{{color}}"
        onChange="onChange" controlled="{{controlled}}"/>