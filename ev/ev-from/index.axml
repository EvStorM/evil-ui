<form data-name="form" size="{{size}}" labelAlign="{{labelAlign}}"
      labelTextAlign="{{labelTextAlign}}" value="{{value}}"
      onChange="onChange" labelCol="{{labelCol}}" class="{{className}}"
      wrapperCol="{{wrapperCol}}">
    <slot></slot>
</form>
